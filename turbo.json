{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "//#deps:check": {
      "cache": false
    },
    "build": {
      "dependsOn": ["//#deps:check", "^build"],
      "outputs": ["dist/**"]
    }
  }
}
